{
  "name": "process-zip-parts",
  "version": "1.0.0",
  "description": "Node.js module to split and upload ZIP file parts",
    "main": "deploy.js",
    "scripts": {
        "start": "node ./deploy.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "axios": "^1.8.4",
        "uuid": "^11.1.0"
    }
}
